/**
 * Client Supabase — PLACEHOLDER (phase 2).
 *
 * La connexion réelle sera branchée en phase 2 (cf. roadmap, note de cadrage §7).
 * Pour l'activer :
 *   1. bun add @supabase/supabase-js
 *   2. renseigner VITE_SUPABASE_URL et VITE_SUPABASE_ANON_KEY dans .env.local
 *   3. décommenter le bloc ci-dessous.
 */

// import { createClient } from '@supabase/supabase-js'
//
// const url = import.meta.env.VITE_SUPABASE_URL
// const anonKey = import.meta.env.VITE_SUPABASE_ANON_KEY
//
// export const supabase = createClient(url, anonKey)

export const supabase = null as unknown
